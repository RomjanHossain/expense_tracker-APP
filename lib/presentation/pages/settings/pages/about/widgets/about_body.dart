import 'package:flutter/material.dart';

/// {@template about_body}
/// Body of the AboutPage.
///
/// Add what it does
/// {@endtemplate}
class AboutBody extends StatelessWidget {
  /// {@macro about_body}
  const AboutBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child:Text('AboutPage'));
  }
}
