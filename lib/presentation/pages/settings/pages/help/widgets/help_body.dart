import 'package:flutter/material.dart';

/// {@template help_body}
/// Body of the HelpPage.
///
/// Add what it does
/// {@endtemplate}
class HelpBody extends StatelessWidget {
  /// {@macro help_body}
  const HelpBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child:Text('HelpPage'));
  }
}
